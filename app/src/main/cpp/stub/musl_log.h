/* Stub: musl_log.h — OHOS HiLog wrapper, not needed on Android. */
#ifndef _MUSL_LOG_H
#define _MUSL_LOG_H
#define MUSL_LOGE(...)
#define MUSL_LOGW(...)
#define MUSL_LOGI(...)
#define MUSL_LOGD(...)
#endif
